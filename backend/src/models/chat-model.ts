/*
    CHAT - 
    id 
    createdAt: date
    updatedAt: date
    lastMessageAt: date
    name?: sting
    isGroup?: boolean

    ?
    messagesIds: string[]
    messages: Messages[]

    MESSAGE - 
    id
    body?: string
    image?: string
    createdAt: date

    seenIds: string[]

    senderId: string
*/
